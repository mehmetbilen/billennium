@* Public SSR navigation. Mobile menu uses <details> for zero-JS disclosure (works in non-interactive SSR). *@


<nav>
    <div class="nav-logo-container">
        <!-- Render your personal logo (PNG file referenced verbatim) -->
        <div class="nav-logo-icon"></div>
        <!-- The text will type itself out, starting AFTER the logo fades in -->
        <NavLink href="" Match="NavLinkMatch.All" class="logo monospace">
            <span id="typewriter-nav"></span><span class="cursor-nav"></span>
        </NavLink>
    </div>
    <div class="nav-links">
        <NavLink href=""
                 Match="NavLinkMatch.All"
                 ActiveClass="active">
            Home
        </NavLink>

        <NavLink href="capabilities"
                 ActiveClass="active">
            Capabilities
        </NavLink>
        <NavLink href="architect"
                 ActiveClass="active">
            The Architect
        </NavLink>
        <NavLink href="initialize"
                 ActiveClass="active">
            Initialize
        </NavLink>
    </div>
</nav>