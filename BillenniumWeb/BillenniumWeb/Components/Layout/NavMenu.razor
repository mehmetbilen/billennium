@* Public SSR navigation. Mobile menu uses <details> for zero-JS disclosure (works in non-interactive SSR). *@

<!-- Navigation with Integrated Logo & CSS-Only Mobile Menu -->
<nav>
    <div class="nav-logo-container">
       <NavLink href="" Match="NavLinkMatch.All" class="logo monospace active" aria-current="page" style="display: flex; align-items: center; gap: 15px;">
            <div class="nav-logo-icon"></div>
            <div style="display: flex; align-items: center; font-weight: 700; letter-spacing: 2px;">
                <span id="typewriter-nav">BILLENNIUM</span>
                <span class="cursor-nav"></span>
            </div>
        </NavLink>
    </div>
   

    <!-- CSS-Only Mobile Menu Checkbox Hack -->
    <input class="menu-btn" type="checkbox" id="menu-btn" />
    <label class="menu-icon" for="menu-btn"><span class="navicon"></span></label>

    <!-- Navigation Links (Changed to a ul/li structure for better mobile formatting) -->
    <div class="nav-links">
        <NavLink href=""
                 Match="NavLinkMatch.All"
                 ActiveClass="active">
            Home
        </NavLink>

        <NavLink href="capabilities"
                 ActiveClass="active">
            Capabilities
        </NavLink>
        <NavLink href="profile"
                 ActiveClass="active">
            Profile
        </NavLink>
        <NavLink href="initialize"
                 ActiveClass="active">
            Initialize
        </NavLink>
    </div>
</nav>